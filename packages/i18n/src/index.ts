// @creed/i18n — mensajes ES por defecto, EN cuando se añada.
// Estructura por namespaces (common, dashboard, chat, log, plan, history, profile, admin, auth, notifications).
// Populated in fase 7 (PWA + i18n + admin).
export {};
